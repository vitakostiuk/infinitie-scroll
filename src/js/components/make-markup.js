// ------ вместо этого используем шаблон -------

// import markupCardTpl from '../templates/markup.hbs'
// export default makeImagesMarkup;

// function makeImagesMarkup(images) {
//   return images.map(markupCardTpl).join('');
// }
